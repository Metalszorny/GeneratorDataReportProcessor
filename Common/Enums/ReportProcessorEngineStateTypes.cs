namespace Common.Enums
{
    /// <summary>
    /// The types for the report processor engine's state.
    /// </summary>
    public enum ReportProcessorEngineStateTypes
    {
        Starting,
        Processing,
        Stopping,
        Stopped
    }
}
